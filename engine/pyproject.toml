[project]
name = "gods-assistant-engine"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "faster-whisper>=1.1.0",
    "kokoro>=0.9.0",
    "groq>=0.25.0",
    "websockets>=14.0",
    "sounddevice>=0.5.0",
    "numpy>=2.0.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
]
